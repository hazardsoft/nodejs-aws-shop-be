import { describe, test, expect } from "vitest";

describe("Tesst for Lambdas", () => {
    test("test #1", () => {
        expect(1 + 2).toBe(3);
    })

    test("test #2", () => {
        expect(1 + 2).toBe(3);
    })
})